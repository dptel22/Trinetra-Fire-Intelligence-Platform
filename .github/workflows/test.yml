name: Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test-backend:
    name: Backend contract tests (app/)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest httpx

      # Contract tests verify real model loading when artifacts exist;
      # they skip gracefully with an informative notice if run without them.
      - name: Run tests
        run: pytest tests/ -v -rs

  strip-notebooks:
    name: Strip Notebook Outputs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install nbstripout
        run: pip install nbstripout

      - name: Check notebooks have no outputs
        run: |
          nbstripout --dry-run notebooks/**/*.ipynb || echo "No notebooks yet or outputs found"
        continue-on-error: true
