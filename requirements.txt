# PS26162 Backend — pinned dependencies.
# Versions locked to Dhruv's training environment (CATBOOST/H3 MUST match the
# model that produced the served .cbm, or predict surfaces drift silently).
catboost==1.2.10
h3==4.5.0
scikit-learn==1.6.1
shap>=0.44
duckdb>=1.0
pydantic>=2.6
fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
pyarrow>=14.0
numpy>=1.26
python-multipart>=0.0.9
pytest>=8.0
httpx>=0.27.0
ruff>=0.16.0
# Live ingestion (FIRMS pull + OSM/WRI static features + state assignment)
requests>=2.31
python-dotenv>=1.0
shapely>=2.0
pyproj>=3.6
osmium>=4.0
pyshp>=2.3
